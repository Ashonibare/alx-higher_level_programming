#include <stdio.h
#include <stdlib.h>
#include <unistd.h>
#include "lists.h"
/**
 *
 * insert a number into a sorted singly linked list.
 * 
 * Return: the address of the new node, or NULL if it failed
 *
 */ 
listin_t *insert_node(listint_t **head, int number)
int main()
{

return (0)
}
